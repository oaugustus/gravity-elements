# Gravity Elements — Spec de Implementação: Etapa 1 (Layout + Element)

> Documento autocontido para implementação por agente de codificação (Cursor AI), nos moldes de `spec-etapa-0-fundacao.md`. Referências completas de arquitetura estão em `gravity-elements-especificacao-tecnica.md`; o processo operacional completo (papéis, fluxo Plan→Build→TODO, sincronização com TickTick) está em `processo-implementacao.md` — este documento extrai e detalha apenas o necessário técnico para a Etapa 1.

## Fluxo de trabalho desta etapa (importante)

Mesmo fluxo da Etapa 0, documentado em detalhe em `processo-implementacao.md`. Resumo:

- Este agente **não tem e não deve buscar acesso** ao TickTick. Progresso é reportado exclusivamente marcando os itens do **TODO da seção 12** deste arquivo.
- **Um chat de Plan mode por item do TODO**, não um chat por componente-e-meio nem por categoria inteira. Cada chat começa sem memória do que veio antes — o prompt de abertura (template `plantask.md`) deve sempre incluir o texto exato da tarefa, a referência de seção deste documento, e instrução para inspecionar o repositório atual antes de planejar.
- Marcar `- [x]` só quando implementado **e verificado** (teste rodou e passou, não só "código escrito"). Sub-linha de evidência obrigatória. Não alterar o texto dos itens — espelham string-a-string as tarefas do TickTick.
- Checkpoint manual adicional já vale a partir desta etapa para os componentes com lógica de verdade: **Calendar** (navegação por teclado), **Collapsible** (abrir/fechar) e **AvatarGroup** (colapso em "+N") — o Otávio confere manualmente no demo app antes de considerar a tarefa concluída, mesmo com a evidência do Cursor já registrada.

## 1. Objetivo

Implementar os 24 componentes das categorias **Layout** e **Element** do Nuxt UI, com paridade visual e de API, usando a fundação da Etapa 0 (`geTv`, `geId`, `geColorMode`, `geOverlayStack`, diretivas de interatividade). Esta é a primeira etapa que produz componentes `ge*` de produção — valida o padrão `component + theme + geTv` em volume antes dos casos complexos de formulário/overlay das etapas seguintes.

Por que Layout+Element primeiro (já decidido em `gravity-elements-plano-etapas.md`): a maioria é estática ou tem estado local simples. Só **Collapsible**, **Calendar** e **AvatarGroup** têm lógica não trivial — os demais 21 são majoritariamente `geTv` + template + bindings.

## 2. Stack e dependências desta etapa

Nenhuma dependência nova de comportamento é necessária — `geTv`, `geId`, `geColorMode` (Etapa 0) e os módulos oficiais `ngAria`/`ngAnimate` já cobrem o que esta etapa precisa. `date-fns` (já instalado na Etapa 0, previsto para uso a partir daqui) entra em uso agora, no **Calendar**.

Uma dependência nova, **só para o demo app** (não é dependência do pacote publicado, não entra no bundle UMD):

- `angular-route` — módulo oficial `ngRoute`, mesma versão `1.8.3` das demais libs `angular-*`, fixada exata (`--save-exact`), como **devDependency** (o demo não é publicado; a biblioteca em si não força roteador nenhum ao app consumidor — ver seção 8).

## 3. Estrutura de pastas a criar

```
gravity-elements/
├── src/
│   ├── components/
│   │   ├── layout/
│   │   │   ├── app/
│   │   │   │   ├── app.component.js
│   │   │   │   ├── app.html
│   │   │   │   ├── app.theme.js
│   │   │   │   └── app.component.spec.js
│   │   │   ├── container/        (mesmos 4 arquivos, padrão <feature>.<tipo>.js)
│   │   │   ├── error/
│   │   │   ├── footer/
│   │   │   ├── header/
│   │   │   ├── main/
│   │   │   ├── sidebar/
│   │   │   ├── theme/
│   │   │   └── layout.module.js
│   │   ├── element/
│   │   │   ├── alert/
│   │   │   ├── avatar/
│   │   │   ├── avatar-group/
│   │   │   ├── badge/            (substitui o smoke throwaway de test/smoke/badge/ — ver §7)
│   │   │   ├── banner/
│   │   │   ├── button/
│   │   │   ├── calendar/
│   │   │   ├── card/
│   │   │   ├── chip/
│   │   │   ├── collapsible/
│   │   │   ├── field-group/
│   │   │   ├── icon/
│   │   │   ├── kbd/
│   │   │   ├── progress/
│   │   │   ├── separator/
│   │   │   ├── skeleton/
│   │   │   └── element.module.js
│   │   └── components.module.js   # agrega layout.module + element.module
│   └── gravity-elements.module.js # passa a incluir 'gravityElements.components'
├── demo/
│   ├── routes.js                  # ngRoute config, 1 entrada por componente
│   └── pages/
│       ├── layout/<nome>.html     # 1 página por componente Layout
│       └── element/<nome>.html    # 1 página por componente Element
```

Nome de pasta = kebab-case do componente (`avatar-group/`, `field-group/`), nome de arquivo = `<feature>.<tipo>.js` (Y070), elemento HTML = `ge-avatar-group` etc.

`src/index.js` (bundle UMD, criado na Etapa 0) passa a importar `components.module.js` e cada `*.component.js`/`*.theme.js` desta etapa — ver critério de aceite 2 na seção 9.

## 4. Convenções obrigatórias (recap da Etapa 0, seção 4 da especificação técnica)

Aplicar em todo arquivo criado nesta etapa, sem exceção:

- Um artefato Angular por arquivo, IIFE (Y001, Y010).
- `controllerAs: 'vm'` em todo componente (Y030–Y032) — nenhum componente novo usa `$scope` clássico.
- Funções nomeadas dentro de `.component()` (Y024).
- Getter syntax encadeada em todo arquivo exceto a declaração do módulo (Y021–Y023).
- Prefixo `ge` no nome do componente (`geButton`, não `suButton` — a Etapa 0 já corrigiu esse resíduo de nome antigo).
- `bindings` documentados via JSDoc no topo do controller, espelhando os nomes de prop do Nuxt UI (`color`, `variant`, `size`...) sempre que possível, para consulta cruzada com `ui.nuxt.com/docs/components/<nome>`.

## 5. Contrato de componente (recap da especificação técnica, seção 5)

Todo componente desta etapa tem, no mínimo, os 4 arquivos: `*.component.js`, `*.html`, `*.theme.js`, `*.component.spec.js`. O exemplo de referência (`button.component.js`/`button.theme.js`) já está na especificação técnica, seção 5 — usar como molde de estrutura (não copiar os valores de classe, que são específicos do Button).

**Algoritmo do tema**: todo `*.theme.js` é `.constant()` no módulo de categoria (`gravityElements.element` ou `gravityElements.layout`), consumido via `geTv` (já implementado e testado na Etapa 0) — não reimplementar resolução de variantes componente a componente.

**Exceção — componentes sem tema upstream**: nem todo componente do Nuxt UI tem um `theme/<nome>.ts` correspondente. Alguns (confirmado para `App` na v4.10.0 — ver seção 6) são só provedores de contexto/configuração, sem template visual ou classes próprias. Nesses casos, **pular o `*.theme.js`** (não criar um arquivo vazio ou inventar classes) — os outros 3 arquivos do contrato (`*.component.js`, `*.html`, `*.component.spec.js`) continuam obrigatórios. Antes de aplicar essa exceção a qualquer componente, confirmar contra a versão fixada na seção 5.1 (não assumir por semelhança de nome) e registrar a confirmação na evidência do TODO (seção 12), citando o que foi checado.

### 5.1 Referência de design — versão fixada

**Referência: Nuxt UI `v4.10.0`** (`github.com/nuxt/ui`, tag/release correspondente a essa versão — confirmar tag exata no GitHub antes de começar; era a última versão publicada no npm em 2026-08-06, data desta spec). Portar quase 1:1 os arquivos `theme/<nome>.ts` dessa versão fixa, não da branch `main`/`latest` a esmo — a Etapa 1 pode se estender por várias sessões de Cursor sem prazo definido (prioridade baixa do projeto), e o Nuxt UI pode publicar novas versões nesse meio-tempo; usar sempre a v4.10.0 como base evita inconsistência visual entre componentes portados em momentos diferentes.

Se, ao longo da etapa, uma nova versão do Nuxt UI for lançada e algum componente específico precisar dela por algum motivo pontual, registrar a exceção explicitamente na evidência do TODO (qual componente, qual versão, por quê) — não trocar a referência da etapa inteira sem atualizar esta seção primeiro.

Nuxt UI v4 unificou Nuxt UI + Nuxt UI Pro (release de setembro/2025) sem reescrever a arquitetura de tema da v3 (migração descrita pelo próprio Nuxt como "effortless", ao contrário do salto v2→v3) — a base já assumida na especificação técnica (Vue 3 + Reka UI + `tailwind-variants`) continua válida.

Cada `*.theme.js` portado leva o cabeçalho de atribuição:
```js
// Portado de github.com/nuxt/ui v4.10.0, MIT License, Copyright (c) Nuxt Labs
```

### 5.2 Padrão de bindings

Bindings simples (`@`, string/enum) para props visuais (`color`, `variant`, `size`), `<` (one-way) para valores/objetos/booleans, `&` para callbacks (`onClick`, `onClose`). Nenhum componente desta etapa usa `require: 'ngModel'` — isso começa na Etapa 2 (Form).

### 5.3 Transclusion

Componentes com conteúdo livre (`geCard`, `geFooter`, `geHeader`, `geFieldGroup`) usam `transclude: true` (ou `transclude: { slotName: '?elementName' }` para multi-slot, disponível desde Angular 1.5) — não reinventar sistema de slots.

### 5.4 Ícones (`geIcon` e uso interno por outros componentes)

O Nuxt UI usa o módulo `Icon` (Iconify) para resolver qualquer coleção de ícones por nome de string. O AngularJS não tem equivalente pronto e o Gravity Elements **não empacota um sistema de ícones próprio** (fora de escopo — ver seção 10). `geIcon` é deliberadamente fino: aplica o binding `name` como classe CSS no elemento (`<i class="{{ vm.name }} {{ vm.classes.base }}"></i>` ou `<span>` equivalente) mais classes de tamanho via `geTv`. Cabe ao app consumidor registrar uma fonte de ícones compatível com classes CSS (ex.: Iconify via `@iconify/tailwind`, Font Awesome, ou qualquer lib que resolva `name` → ícone visível). Documentar essa decisão no README do componente e no `*.component.js` via comentário.

**Componentes que usam `icon` antes de `geIcon` existir**: pela ordem do TODO (seção 12), vários componentes com binding `icon` (`geError`, `geAlert`, `geBanner`...) são implementados antes da tarefa `Componente: Icon`. Não bloquear nem reordenar o TODO por causa disso — aplicar a mesma classe CSS diretamente no template do componente (o que `geIcon` faria, só inline), com um comentário no `*.component.js` marcando que deve ser trocado por `<ge-icon>` quando a tarefa `Componente: Icon` for concluída.

### 5.4.1 Sub-componentes ainda não implementados (ex.: botões dentro de outro componente)

Alguns componentes do Nuxt UI usam `UButton` internamente (ex.: `Error` tem um botão "voltar"). Pela ordem do TODO, `geButton` só é implementado na tarefa 14 — não vale a pena adiar nem reordenar tarefas anteriores por causa disso (quebraria a sequência Layout→Element já fixada no TickTick). Nesses casos, usar `<button>` nativo com classes Tailwind estáticas aproximando os valores default do `UButton` correspondente (tamanho, cor, variante), documentado com um comentário indicando que deve ser revisitado para usar `<ge-button>` depois que a tarefa `Componente: Button` estiver concluída — mesmo padrão do `geApp` (stub revisitado na Etapa 4).

### 5.4.2 Bindings além do resumo das seções 6/7

As tabelas das seções 6 e 7 listam os bindings principais de cada componente como ponto de partida, não um contrato fechado — a seção 4 já manda espelhar os nomes de prop do Nuxt UI "sempre que possível". Se, ao consultar a versão fixada (seção 5.1), o componente real tiver props adicionais com slot correspondente no `theme/<nome>.ts` (ex.: `message`/`leadingIcon` em `Error`), incluir esses bindings também — não vale criar um tema com slots que o componente nunca usa (mesmo raciocínio, ao contrário, da exceção de tema ausente da seção 5). Registrar na evidência do TODO quais bindings foram adicionados além da tabela e por quê.

### 5.5 Acessibilidade (ARIA) por componente

`ngAria` (incluído em `gravityElements.core` desde a Etapa 0) cobre automaticamente `aria-disabled`/`aria-required`/`aria-checked`/`aria-invalid` quando os bindings correspondentes (`ng-disabled`, `ng-required`, `ngModel`) são usados — mas **não cobre roles nem atributos específicos de widget**, que precisam ser adicionados manualmente no template de cada componente. Mínimo obrigatório nesta etapa:

| Componente | ARIA mínimo |
|---|---|
| `geAlert` | `role="alert"` no elemento raiz. |
| `geBanner` | `role="status"` (ou `role="alert"` se `color` indicar erro/warning — decisão do Cursor, documentar). |
| `geAvatar` | `alt` obrigatório no `<img>` quando `src` é usado; `aria-hidden="true"` no fallback de ícone/iniciais quando não há texto acessível equivalente. |
| `geAvatarGroup` | `aria-label` no avatar `+N` (ex. `"mais 3"`), não só texto visual. |
| `geButton` | `aria-busy="true"` quando `loading` (além do `aria-disabled` automático via `ngAria`/`ng-disabled`). |
| `geCollapsible` | `aria-expanded` no elemento de trigger, `aria-controls` apontando pro id do painel (via `geId`, Etapa 0), painel com `aria-hidden` quando fechado. |
| `geCalendar` | Grid de dias com `role="grid"`/`role="row"`/`role="gridcell"` (ou `role="button"` por dia, se optar por estrutura mais simples — documentar a escolha), `aria-selected` no dia selecionado, `aria-label` por dia com data completa (via `date-fns format`), região do mês/ano com `aria-live="polite"` para anunciar troca de mês. |
| `geProgress` | `role="progressbar"`, `aria-valuenow`/`aria-valuemin`/`aria-valuemax`. |
| `geSeparator` | `role="separator"`, `aria-orientation` espelhando o binding `orientation`. |
| `geSkeleton` | `aria-hidden="true"` (é só placeholder visual, não deve ser anunciado por leitor de tela). |
| `geSidebar` | Botão de toggle com `aria-expanded` refletindo `collapsed`. |
| `geTheme` | Botão de troca de tema com `aria-label` descritivo (ex. `"Alternar tema claro/escuro"`) — não depender só de ícone. |

Demais componentes da tabela das seções 6/7 sem ARIA especificado aqui não têm requisito adicional além do que `ngAria` já cobre automaticamente.

### 5.6 Casos de teste mínimos — componentes com lógica

Os demais componentes (a maioria) seguem o padrão já validado na Etapa 0 para `geBadge`: pelo menos 2 casos por componente (defaultVariants e 1 override de prop relevante) já satisfazem o contrato. Os três com lógica de verdade precisam de mais:

- **`geCalendar`**: mínimo 5 casos — navegação por seta (foco move um dia), `Home`/`End` (início/fim da semana), `PageUp`/`PageDown` (troca de mês, com o mês exibido atualizado), `Enter`/`Espaço` seleciona o dia focado e dispara `onUpdate`, e um caso de limite (`minDate`/`maxDate` desabilita navegação além do intervalo).
- **`geCollapsible`**: mínimo 3 casos — abre e aplica `aria-expanded="true"` + remove `aria-hidden` do painel, fecha e reverte, e um caso com `disabled: true` confirmando que o toggle não muda o estado.
- **`geAvatarGroup`**: mínimo 3 casos — menos que `max` avatares (todos visíveis, sem "+N"), exatamente `max` (todos visíveis, sem "+N"), mais que `max` (excedente escondido, avatar "+N" com o número certo e `aria-label` correspondente).

**Verificação de CSS compilado (obrigatória quando aplicável).** Os casos acima comparam a string de classe calculada pelo `geTv` contra o próprio `geTv` (`root.className === expected.root`) — isso valida a *lógica de resolução de variantes*, não que o Tailwind de fato gerou CSS pra essas classes. `npm run build:css` sair com exit 0 **não** garante isso: o Tailwind descarta silenciosamente, sem erro nem aviso, qualquer classe com sintaxe que não reconhece (ver checklist da seção 5.7). Já aconteceu três vezes nesta etapa (`window.twMerge` nunca carregado no bundle publicado — Container; `bg-[var(...)]/N` sem CSS gerado — Alert; `outline-3`/`-outline-offset-3` sem CSS gerado — Banner), sempre com `npm test` e `npm run lint` passando normalmente. Por isso, para todo componente cujo `*.theme.js` use qualquer padrão da seção 5.7 (opacidade sobre `var()`, `ring`/`outline` fora da escala 0/1/2/4/8, variantes `not-*`, ou qualquer outro valor arbitrário não trivial), a evidência do TODO (seção 12) precisa registrar, além do `npm test`/`npm run lint`/`npm run build:*`: build isolado do Tailwind CLI (`npx tailwindcss -i ... -o ...`, sem `--minify` pra facilitar inspeção) e confirmação, por busca direta no CSS gerado, de que pelo menos a combinação default e uma combinação não trivial (ex.: uma variante com opacidade) produzem declaração real (`background-color:`/`--tw-ring-color:`/`outline-width:` etc.), não só a presença do token na safelist.

**Caso pós-montagem (quando o componente tem estado que pode mudar depois do `$onInit`).** Os 2 casos mínimos genéricos testam só o estado no momento da montagem (default + 1 override, ambos resolvidos no compile inicial). Componentes com `$onChanges`, `$doCheck`, ou qualquer mecanismo de propagação pra filhos (`require`, `$transclude`, dados compartilhados via controller pai) precisam de pelo menos 1 caso adicional mudando uma prop relevante **depois** da montagem inicial (`scope.prop = novoValor; scope.$digest();`) e confirmando que o DOM reflete a mudança — ou, se a limitação for arquitetural e aceita (ex.: um valor só é lido uma vez no `$onInit` e não se propaga depois), documentar isso explicitamente na evidência do TODO como comportamento conhecido, não deixar sem menção.

### 5.7 Padrões Tailwind v4 que não compilam no Tailwind 3.4.19 deste projeto (checklist obrigatório)

O Nuxt UI v4.10.0 (referência fixada na seção 5.1) assume Tailwind CSS v4 nos arquivos `theme/*.ts`. Este projeto usa Tailwind v3.4.19 (seção 2 da especificação técnica) — alguns padrões de classe do v4 não têm equivalente direto e precisam de adaptação manual, não só troca de token semântico por `var()`. Os três já confirmados (cada um via build isolado do Tailwind CLI, reproduzível fora do jsdom):

1. **Opacidade sobre `var()` (`bg-primary/10`, `ring-color/25` etc.)** — o modificador `/N` do Tailwind v4 não compila quando o valor de cor é uma `var()` arbitrária no v3.4.19 deste projeto (mesmo com a classe na safelist). Adaptação: escrever o `color-mix()` por extenso como valor arbitrário completo — `bg-[color-mix(in_srgb,var(--ui-primary)_10%,transparent)]` em vez de `bg-[var(--ui-primary)]/10`. Confirmado funcionando pra `bg-`, `ring-` e `outline-`. Precedente: `header.theme.js` (`bg-default/75`, correto desde o início), corrigido depois em `alert.theme.js` (bug real, 18 combinações sem cor) e generalizado em `badge.theme.js`/`banner.theme.js`.
2. **`ring-N`/`outline-N` fora da escala fixa do v3 (`ring-3`, `outline-3`)** — o Tailwind v3 só tem os passos 0/1/2/4/8 pra largura de `ring`/`outline`; o v4 aceita qualquer número. Adaptação: se o valor upstream cair exatamente num desses passos, usar a utilidade padrão (`ring-3` do v4 é literalmente 3px, que é o `ring` **sem sufixo** do v3 — não `ring-3`); se não cair, usar valor arbitrário em colchetes (`outline-[3px]`, `-outline-offset-[3px]`). Confirmado bug real em `banner.theme.js` (variant `to`); `sidebar.theme.js` já tinha usado a forma correta (`ring-3` do v4 → `ring` do v3) desde o início.
3. **Variantes `not-*` (`not-only:`, `not-first:`, `not-last:`)** — negação de variante, feature exclusiva do Tailwind v4; não existe no v3.4.19, nem como plugin padrão. Não há forma de "escrever por extenso" como no caso 1 — precisa reescrever como seletor arbitrário (`[&:not(:only-child):first-child]:...`) ou aceitar que a classe fica inerte até uma migração de Tailwind. Confirmado em `badge.theme.js` (`fieldGroup` variant, hoje nunca ativado pelo controller — inofensivo por enquanto, mas vai quebrar silenciosamente se algum componente futuro reusar esse padrão de tema achando que funciona).

Antes de portar qualquer `compoundVariants`/`variants` com classes fora do básico (`px-4`, `flex`, cores nomeadas do Tailwind), checar se a classe usa algum desses três padrões. Na dúvida, testar isolado (`npx tailwindcss -i entrada.css -o saida.css` com um `content` mínimo apontando só pra classe em questão) antes de assumir que compila.

### 5.8 Nota: `ngAnimate` e classes transitórias em testes

`gravityElements.core` depende de `ngAnimate` (Etapa 0). Qualquer elemento controlado por uma diretiva estrutural (`ng-if`, `ng-repeat`, `ng-switch`) — inclusive o próprio elemento raiz de um componente, não só filhos internos — recebe classes transitórias (`ng-animate`, `ng-enter`/`ng-enter-active`, `ng-leave`/`ng-leave-active`) que só são removidas de forma assíncrona (mesmo sem nenhuma transição CSS definida pra elas). Um teste que sincronicamente chama `scope.$digest()` uma vez e imediatamente compara `element.className` pode ver essas classes extras ainda presentes. Se um caso de teste envolver o elemento raiz do próprio componente atrás de `ng-if` (padrão usado em `geAlert`, `geBanner`, e implicitamente no `<img>`/troca de estado do `geAvatar`), aguardar um tick antes da asserção final (ex.: `$timeout` de 0, ou um segundo `$digest()` após um `setTimeout`) evita falso negativo. Relevante principalmente para `geCollapsible` (ainda não implementado), que usa `ngAnimate` de propósito para a transição de altura — os 3 casos mínimos da seção 5.6 devem levar isso em conta.

**Atualização (2026-08-08, revisão do `$onChanges` em Button/Alert/Badge/Banner):** o efeito não fica restrito a elementos atrás de `ng-if`. Qualquer atributo `class="{{ vm.classes.* }}"` interpolado — em **qualquer** elemento, mesmo sem diretiva estrutural — passa pelo `$animate` quando `ngAnimate` está carregado, que despacha a troca como `addClass`/`removeClass` assíncrono. Em jsdom, isso pode ficar preso indefinidamente num estado intermediário com classes sufixadas `-add`/`-remove` (ex.: `bg-[var(--ui-primary)]-remove ... bg-[var(--ui-error)]-add`) mesmo depois de múltiplos `$digest()` e um `setTimeout(fn, 0)` — um único tick de 0ms nem sempre é suficiente (confirmado: `geButton` precisou de *polling* por até ~150ms pra resolver; o *leave* do `ng-if` do `geAlert`/`geBanner` ao fechar precisou de ~50ms). Duas consequências práticas pra quem for escrever os casos pós-montagem exigidos pela seção 5.6:

1. **Preferir asserir contra o valor computado pelo controller (`element.isolateScope().vm.classes.base`, ou o retorno de `geTv(...)` esperado), não contra `element.className` do DOM**, sempre que o teste for sobre mudança de classe pós-montagem. É mais rápido, determinístico, e não depende de quantos ticks o `$animate` do jsdom precisa. Precedente: o caso pós-montagem do `geBadge` (`badge.component.spec.js`) já faz isso corretamente.
2. Quando o teste *precisa* verificar o DOM em si (ex.: `role`, `aria-*`, presença/ausência de um elemento por `ng-if`), usar um *poll* com múltiplos `$digest()` intercalados por um intervalo real (não confiar em um único `setTimeout(fn, 0)`) até a condição ser satisfeita ou um timeout generoso (≥1s) estourar.

Isso não é uma regressão de comportamento nem um bug de produto — é uma limitação de timing do `$animate` no ambiente jsdom (sem `requestAnimationFrame`/CSS engine reais) desta suíte de verificação fora do Karma; o comportamento real em Chrome (Karma) não foi confirmado nesta sessão (mesma ressalva já registrada acima).

### 5.9 Auditoria retroativa das seções 5.6–5.8 contra os componentes já implementados (2026-08-08)

As seções 5.6–5.8 foram escritas depois de já ter 13 componentes implementados e revisados (App até Banner). Auditoria feita componente a componente contra os três itens novos, pra decidir o que precisa de retrabalho:

- **Checklist 5.7 (padrões que não compilam)**: `geApp`, `geContainer`, `geError`, `geFooter`, `geMain` não usam nenhum dos três padrões (sem opacidade sobre `var()`, sem `ring`/`outline` fora da escala, sem `not-*`) — nada a fazer. `geHeader` já usava `color-mix()` corretamente desde a implementação original (precedente, não bug). `geSidebar` já tinha adaptado `ring-3`→`ring` corretamente desde a implementação original. `geTheme` não tem `*.theme.js`. `geAlert` e `geBanner` tinham bug real (opacidade e escala de `outline`, respectivamente) — **já corrigidos e revalidados** (evidência nas seções correspondentes do TODO). `geAvatar` e `geAvatarGroup` já usavam `color-mix()`/`ring` corretamente. `geBadge` tem um caso real do padrão 3 (`not-only:`/`not-first:` no variant `fieldGroup`), mas é código inerte — o controller nunca passa esse variant pro `geTv` até `geFieldGroup` existir; não é um bug ativo, só dívida técnica documentada. **Nenhum retrabalho de código pendente neste item** — só atenção quando `geFieldGroup` for implementado, pra não repetir o padrão quebrado.
- **Verificação de CSS compilado (5.6)**: já foi feita manualmente (fora do Karma, via build isolado do Tailwind CLI) pra todos os 13 componentes já implementados, como parte do processo de revisão — é assim que os bugs do Alert e do Banner foram encontrados. A exigência formalizada nesta seção **não é retroativa** (os componentes já implementados já passaram por ela, ainda que o processo não estivesse escrito na spec no momento); vale a partir daqui como parte do fluxo normal de toda tarefa nova.
- **Caso pós-montagem (5.6)**: nenhum dos `*.component.spec.js` já commitados dos 13 componentes tem um caso oficial de mudança de prop pós-montagem — os únicos testes desse tipo que existem foram escritos ad hoc durante a revisão (fora do repositório, não persistidos). Recomendação, não bloqueio: ao revisitar `geAvatar` (fallback de erro de imagem + reset ao trocar `src`) e `geAvatarGroup` (mudança dinâmica de `max`) no futuro, considerar promover esses casos pro `*.component.spec.js` oficial. Também durante essa revisão foi confirmada uma limitação real e não coberta por teste nenhum: se `size`/`color` do `geAvatarGroup` mudar depois que os avatares filhos já renderizaram, os filhos existentes não atualizam (só leem o valor do grupo uma vez, no `$onInit` — diferente do Vue original, que propaga via `provide`/`inject` reativo). Decisão pendente do Otávio: aceitar como limitação documentada (registrar em `geAvatarGroup`'s JSDoc + evidência) ou tratar como bug a corrigir. Não bloqueia nada por enquanto.
- **Nota `ngAnimate` (5.8)**: `geAlert`, `geBanner` e o fluxo de erro de imagem do `geAvatar` têm elemento atrás de `ng-if` e precisaram de um tick extra pra assentar durante a revisão fora do Karma (jsdom). Não se sabe com certeza se o Karma real (Chrome Headless) sofre do mesmo atraso — não foi possível confirmar neste ambiente (sandbox sem Chrome). Os testes oficiais desses componentes passam conforme reportado pelo Cursor (`npm test`); não há evidência concreta de flakiness real. **Sem ação necessária agora** — só relevante se algum teste desses começar a falhar de forma intermitente no CI/local, aí revisitar com o padrão de tick extra descrito na seção 5.8.

### 5.10 Nota: `ng-attr-data-*`/`ng-attr-*` nunca resolve se colidir com `BOOLEAN_ATTR` do AngularJS (checklist obrigatório, achado na revisão do Calendar, 2026-08-08)

Confirmado via leitura direta do `$compile.js` do AngularJS 1.8.3 (`node_modules/angular/angular.js`) e reproduzido em isolamento total (sem jsdom-specifics, sem `ngAria`/`ngAnimate`, puro `$compile`): quando um atributo `ng-attr-<algo>-<palavra>` é usado e a **última palavra** do nome (depois de qualquer prefixo, incluindo `data-`/`x-` no meio do nome) coincide com uma das sete chaves de `BOOLEAN_ATTR` do AngularJS — `multiple`, `selected`, `checked`, `disabled`, `readOnly`, `required`, `open` — **e** o elemento é um dos oito tipos em `BOOLEAN_ELEMENTS` — `input`, `select`, `option`, `textarea`, `button`, `form`, `details` — o AngularJS trata o atributo inteiro como booleano nativo (equivalente a escrever `ng-attr-disabled`, não `ng-attr-data-disabled`) e tenta setar uma **propriedade DOM** (`element.prop('selected', ...)`) em vez do atributo literal pedido. Como o elemento não tem essa propriedade IDL nativa (ex.: `<button>` não tem `.selected`), nada visível acontece: nenhum atributo é escrito no DOM, sem erro, sem aviso — mesma categoria de falha silenciosa dos padrões do Tailwind na seção 5.7, mas na camada do AngularJS, não do CSS.

**Caso real encontrado**: `calendar.component.js`/`calendar.html` usavam `ng-attr-data-selected="{{ day.selected ? 'true' : undefined }}"` e `ng-attr-data-disabled="{{ day.disabled ? 'true' : undefined }}"` no botão de cada dia — o atributo `data-selected`/`data-disabled` **nunca era escrito no DOM**, mesmo com `day.selected`/`day.disabled` corretamente `true` no controller. Isso quebrava silenciosamente toda a estilização condicional do tema que dependia de `data-[selected]:`/`data-[disabled]:` (destaque visual do dia selecionado, texto apagado do dia desabilitado) — apesar de `aria-selected` (no `gridcell` pai, via `ng-attr-aria-selected`) e do `disabled` nativo (via `ng-disabled` separado, não via `ng-attr-data-disabled`) continuarem funcionando normalmente, o que mascarava o problema em qualquer verificação que só olhasse ARIA/clicabilidade e não a classe visual renderizada. Confirmado reproduzível em isolamento completo (fora do componente, fora do jsdom-specific setup) — não é um artefato de ambiente, ocorre em qualquer AngularJS 1.8.3 real, incluindo Karma/Chrome.

**Adaptação confirmada**: renomear o atributo pra uma palavra que não colida com `BOOLEAN_ATTR` — ex. `data-selected` → `data-is-selected`, `data-disabled` → `data-is-disabled` (mantendo o `ng-attr-data-is-selected`/`ng-attr-data-is-disabled` no template) — resolve completamente; confirmado via teste isolado que o atributo passa a ser escrito/removido corretamente em todos os digests subsequentes. Qualquer classe do tema que dependa do nome antigo via seletor arbitrário do Tailwind (`data-[selected]:...`, `[&[data-today]:not([data-selected])]:...` etc.) precisa ser atualizada em conjunto pro novo nome (`data-[is-selected]:...`).

Antes de usar `ng-attr-data-<palavra>` (ou qualquer `ng-attr-*` cuja última palavra seja um desses sete nomes) num elemento `input`/`select`/`option`/`textarea`/`button`/`form`/`details`, checar contra essa lista. Na dúvida, testar isolado (compilar um `<button ng-attr-data-<palavra>="{{ true }}">` fora do componente e checar `getAttribute`) antes de assumir que o atributo aparece no DOM.

## 6. Componentes — Layout (8)

| Componente | Bindings principais | Notas |
|---|---|---|
| `geApp` | — (sem bindings próprios) | **Sem `geApp.theme.js`** — confirmado que `App` não tem `theme/app.ts` na v4.10.0 (ver seção 5, exceção); é um componente provedor de contexto (Toast/Tooltip/overlays programáticos via Reka UI `ConfigProvider` no original), não visual. Nesta etapa, `geApp` é necessariamente um **stub** desse papel: só container raiz + transclusion, aplicando no `$onInit` o modo persistido de `geColorMode` (`get()`) — dogfooding do serviço da Etapa 0. O papel completo de provedor (Toast/overlays programáticos) só faz sentido a partir da Etapa 4 (Overlay), quando `geOverlayStack` e um futuro `geToast` existirem de verdade — revisar `geApp` nessa etapa, não tratar o resultado desta tarefa como versão final. |
| `geContainer` | `size` (`@`, opcional, largura máxima) | Wrapper simples de centralização/padding; transclusion. |
| `geError` | `statusCode` (`@`), `statusMessage` (`@`), `clear` (`<`, boolean — mostra botão), `onClear` (`&`) | Página de erro genérica (404/500), paridade com `Error` do Nuxt UI. |
| `geFooter` | — | **Multi-slot** (decisão registrada em 2026-08-07, §5.4.2): Nuxt UI v4.10.0 `Footer` tem slots `top`/`left`/`default`/`right`/`bottom` e tema com `root`/`top`/`bottom`/`container`/`left`/`center`/`right` — portar 1:1, não o wrapper de slot único descrito originalmente aqui. `transclude: { top: '?geFooterTop', left: '?geFooterLeft', right: '?geFooterRight', bottom: '?geFooterBottom' }` + conteúdo default (`center`) via transclusion simples, seguindo o padrão multi-slot já previsto na seção 5.3. |
| `geHeader` | `title` (`@`, opcional) | Transclusion com slot para ações à direita (`transclude: { title: '?geHeaderTitle', right: '?geHeaderRight' }` ou binding `title` + transclusion simples do restante — decisão livre do Cursor, documentar a escolhida). |
| `geMain` | — | Wrapper semântico (`<main>`), sem lógica. |
| `geSidebar` | `side` (`@`, `'left'`\|`'right'`, default `'left'`), `collapsible` (`@`, `'offcanvas'`\|`'icon'`\|`'none'`, default `'none'`), `variant` (`@`, `'sidebar'`\|`'floating'`\|`'inset'`, default `'sidebar'`), `open` (`<`, two-way via `=`), `onToggle`/`onOpenChange` (`&`) | **Tema desktop quase completo** (decisão registrada em 2026-08-06, §5.4.2): Nuxt UI v4.10.0 `Sidebar` tem tema com slots `root`/`gap`/`container`/`inner`/`header`/`wrapper`/`title`/`description`/`actions`/`close`/`body`/`footer`/`rail` e variantes `side`/`collapsible`/`variant`/`transition` — portar o tema e o comportamento desktop (`side`, `collapsible`, `variant`, transições), seguindo o mesmo precedente do `geHeader`: overlay/drawer mobile fica fora de escopo (Etapa 4, quando `geModal`/`geDrawer` existirem). `rail` (faixa fina sempre visível quando colapsado) também fica fora desta tarefa — é aditivo e não força mudança de API depois. Botão de toggle sem `geIcon`/`geButton`: usar `<button>` nativo com classe do slot `close`/`trigger`, mesmo padrão do placeholder já aceito em `geError` (§5.4.1). **Pré-requisito de infraestrutura**: o `geTv` atual (`core/tv/tv.service.js`) faz match estrito em `compoundVariants` e não resolve condições com array de valores (ex.: `collapsible: ['offcanvas', 'icon']` como OR) — estender `compoundMatches` para aceitar array faz parte do escopo desta tarefa, com cobertura própria em `tv.service.spec.js` (mínimo: 1 caso que ativa o compound via valor do array e 1 caso que não ativa). ARIA: ver seção 5.5. |
| `geTheme` | `mode` (`<`, opcional — controla `geColorMode` de fora) | Único componente desta etapa que integra `geColorMode` (Etapa 0) diretamente; expõe toggle claro/escuro básico. Variantes de outros componentes por tema (`ColorModeButton`/`Select`/`Switch`) ficam fora do escopo da v1 (especificação técnica, seção 12). ARIA: ver seção 5.5. |

## 7. Componentes — Element (16)

| Componente | Bindings principais | Notas |
|---|---|---|
| `geAlert` | `title` (`@`), `description` (`@`), `color` (`@`), `variant` (`@`), `icon` (`@`), `closable` (`<`), `onClose` (`&`) | Usa `geIcon` internamente se `icon` for passado. ARIA: ver seção 5.5. |
| `geAvatar` | `src` (`@`), `alt` (`@`), `text` (`@`, fallback iniciais), `icon` (`@`, fallback ícone), `size` (`@`), `chipColor`/`chipPosition` (`@`, indicador de status) | Fallback: `src` → `text` → `icon`, nessa ordem, resolvido no controller. ARIA: ver seção 5.5. |
| `geAvatarGroup` | `max` (`<`, número), `size` (`@`, propaga para os avatares filhos) | Transclusion de múltiplos `<ge-avatar>`; no `$onInit`/`$postLink`, conta os filhos, aplica overlap (classe de margem negativa via tema) e substitui o excedente (`> max`) por um avatar `+N`. Casos de teste mínimos: seção 5.6. |
| `geBadge` | `label` (`@`), `color` (`@`), `variant` (`@`), `size` (`@`) | **Promove o smoke test da Etapa 0** (`test/smoke/badge/`) à versão de produção em `src/components/element/badge/`. O smoke era deliberadamente mínimo (só `color`); esta versão precisa do contrato completo (`variant`, `size`, paridade total com `ui.nuxt.com/docs/components/badge`). Depois de migrado, apagar `test/smoke/badge/` (era descartável por definição — seção 6.7 da spec da Etapa 0) e remover as 3 linhas de `test/karma.conf.js` que carregavam o smoke module. |
| `geBanner` | `title` (`@`), `icon` (`@`), `color` (`@`), `closable` (`<`), `onClose` (`&`) | Transclusion opcional para ações extras. ARIA: ver seção 5.5. |
| `geButton` | `label` (`@`), `color` (`@`), `variant` (`@`), `size` (`@`), `block` (`<`), `square` (`<`), `loading` (`<`), `disabled` (`<`), `onClick` (`&`) | Exemplo de referência já dado na especificação técnica, seção 5 — implementar literalmente esse contrato (mais leading/trailing icon via `geIcon`, se aplicável). ARIA: ver seção 5.5. |
| `geCalendar` | `modelValue` (`<`, Date), `onUpdate` (`&`), `minDate` (`<`), `maxDate` (`<`), `locale` (`@`, opcional) | **Componente não trivial.** Grid de dias do mês com roving tabindex (`tabbable`, mesma lib da Etapa 0); navegação: setas movem foco dia-a-dia, `Home`/`End` vão para início/fim da semana, `PageUp`/`PageDown` trocam de mês, `Enter`/`Espaço` selecionam. Matemática de datas via `date-fns` (`startOfMonth`, `endOfMonth`, `addMonths`, `format`, `isSameDay`) — não reimplementar cálculo de calendário à mão. ARIA e casos de teste mínimos: seções 5.5/5.6. |
| `geCard` | — | Multi-slot via `transclude: { header: '?geCardHeader', body: '?geCardBody', footer: '?geCardFooter' }` (ou slot único se o Nuxt UI v4.10.0 original for mais simples — conferir `ui.nuxt.com/docs/components/card` antes de decidir). |
| `geChip` | `text`/`label` (`@`), `color` (`@`), `size` (`@`), `position` (`@`), `standalone` (`<`) | Paridade com indicador de notificação do Nuxt UI (usado sozinho ou sobre outro elemento). |
| `geCollapsible` | `modelValue` (`<`, boolean — aberto/fechado), `onUpdate` (`&`), `disabled` (`<`) | **Componente não trivial.** Transição de altura via `ngAnimate` (`ng-if`/`ng-show` com classes `.ge-collapsible-enter`/`.ge-collapsible-leave`, já que `ngAnimate` está incluído em `gravityElements.core` desde a Etapa 0) — não escrever animação JS própria. ARIA e casos de teste mínimos: seções 5.5/5.6. |
| `geFieldGroup` | `size` (`@`, propaga para os filhos) | Agrupamento visual de inputs/botões adjacentes (bordas coladas). Só o wrapper nesta etapa — inputs de verdade nascem na Etapa 2. |
| `geIcon` | `name` (`@`), `size` (`@`) | Ver seção 5.4 — aplica `name` como classe CSS, sem sistema de ícones embutido. |
| `geKbd` | `value` (`@`) ou `keys` (`<`, array) | Tabela estática de símbolos para teclas especiais (`cmd` → `⌘`, `shift` → `⇧` etc.), paridade com `Kbd` do Nuxt UI. |
| `geProgress` | `value` (`<`), `max` (`<`, default 100), `color` (`@`), `size` (`@`), `status` (`<`, boolean — mostra label de %) | Barra simples; sem animação de indeterminate nesta etapa a menos que o Nuxt UI v4.10.0 original tenha (conferir antes de implementar). ARIA: ver seção 5.5. |
| `geSeparator` | `orientation` (`@`, `'horizontal'`\|`'vertical'`), `label` (`@`, opcional), `color` (`@`), `size` (`@`), `type` (`@`, `'solid'`\|`'dashed'`) | Sem lógica além de `geTv`. ARIA: ver seção 5.5. |
| `geSkeleton` | — | Só classes de tema (animação `pulse` via CSS/Tailwind, sem JS). ARIA: ver seção 5.5. |

## 8. Demo app: 1 rota por componente

Adotar `ngRoute` (módulo oficial do AngularJS, mesma escolha "oficial primeiro" já usada para `ngAria`/`ngAnimate`/`ngMessages` na Etapa 0/2) só no `demo/`, como devDependency:

- `npm install angular-route@1.8.3 --save-exact --save-dev`.
- `demo/routes.js`: `$routeProvider.when('/layout/app', { templateUrl: 'demo/pages/layout/app.html' })` etc., uma entrada por componente (24 rotas), `otherwise` redirecionando para a primeira.
- Uma página HTML por componente em `demo/pages/<categoria>/<nome>.html`, com pelo menos: uso básico e 2–3 variações de props relevantes visíveis lado a lado (facilita a comparação visual do critério de aceite).
- `demo/index.html` ganha `ng-view` + navegação lateral simples (lista de links para as 24 rotas; pode usar o próprio `geSidebar` assim que estiver pronto — dogfooding, não obrigatório).
- Isso é aditivo ao demo existente da Etapa 0 (`demo/app.js`, `demo/smoke-tooltip.*`) — não remover o que já está lá, só estender.

## 9. Critérios de aceite (verificar e relatar todos)

1. Cada um dos 24 componentes: os arquivos do contrato completos (seção 5 — 4 arquivos, ou 3 nos casos documentados de exceção sem tema upstream, como `geApp`), teste unitário Karma passando com o mínimo de casos aplicável (seção 5.6 para `geCalendar`/`geCollapsible`/`geAvatarGroup`, 2 casos para os demais), ARIA mínimo aplicado onde exigido (seção 5.5), `eslint .` limpo.
2. `src/index.js` atualizado para importar `components.module.js` e cada `*.component.js`/`*.theme.js` desta etapa; `npm run build:js` (Rollup) continua gerando `dist/gravity-elements.umd.js` sem erro, com os 24 componentes registrados em `gravityElements.components` (verificar via injector, como já feito na Etapa 0 para `geId`/`geColorMode`).
3. `npm run build:css` (Tailwind) gera `dist/gravity-elements.css` sem erro **e** a safelist (`tailwind.safelist.json`) captura de fato as classes de todos os `*.theme.js` novos — esta é a primeira vez que `generate-tailwind-safelist.js` roda contra temas reais (na Etapa 0 só foi validado com 0 temas). Se algum tema do Nuxt UI usar classe montada dinamicamente (ex. `` `bg-${color}-500` ``), confirmar manualmente que a classe final aparece no CSS gerado; se o script não capturar, ajustar o script ou listar a classe explicitamente no `tailwind.config.js` (`safelist` estático) e registrar a exceção na evidência. **Importante**: "build sem erro" não é o mesmo que "CSS correto" — o Tailwind não avisa quando descarta silenciosamente uma classe com sintaxe que não reconhece (checklist da seção 5.7). A safelist conter o token não garante que existe uma regra real no CSS gerado para ele; ver seção 5.6 (verificação de CSS compilado).
4. Demo app com as 24 rotas navegáveis (`npm run demo`), sem erro no console do navegador.
5. `npm test` (Karma) continua passando 100%, incluindo toda a suíte herdada da Etapa 0 (nenhuma regressão) — reportar a contagem final de testes.
6. [Critério de aceite] Comparação visual com `ui.nuxt.com` (versão v4.10.0 fixada na seção 5.1) sem divergência perceptível, componente a componente.
7. [Critério de aceite] Calendar navegável por teclado — os 5 casos da seção 5.6 passando, mais confirmação manual no demo app (checkpoint da seção "Fluxo de trabalho").

## 10. Fora de escopo desta etapa

Componentes de Form, Data, Navigation, Overlay e Dashboard (Etapas 2–6). Sistema de ícones embutido (só a classe-passthrough da seção 5.4). Variantes de Color Mode por componente (`ColorModeButton`/`Select`/`Switch` — v2, especificação técnica seção 12). Qualquer uso de `ge-floating-position`/`ge-focus-trap`/`ge-hotkey` (isso é Overlay, Etapa 4 — nenhum componente desta etapa precisa deles). Papel completo de `geApp` como provedor de Toast/overlays programáticos (equivalente ao `ConfigProvider`/`ToastProvider` do `UApp` original) — nesta etapa `geApp` é só um stub (seção 6); revisar na Etapa 4.

## 11. Sobre o mapeamento com o TickTick

O TODO da seção 12 é um espelho exato (mesmo texto, **incluindo o typo já existente no TickTick** — "Calendar navagável", não "navegável") das 27 tarefas do projeto "Etapa 1 - Layout + Element" no TickTick, já reordenadas na sequência correta (8 Layout, depois 16 Element, depois Demo app e os 2 critérios de aceite): 24 tarefas `Componente: <Nome>` (uma por componente, cobrindo os 4 artefatos do contrato + teste unitário passando cada uma, conforme critério 1 da seção 9), mais "Demo app: 1 rota por componente desta etapa" e os dois critérios de aceite finais da seção 9 (itens 6 e 7). Cada tarefa `Componente: X` só deve ser marcada `[x]` quando os 4 arquivos existirem, o teste unitário passar com o mínimo de casos exigido e o `eslint .` estiver limpo — não basta o `.component.js` existir.

## 12. TODO (espelho das tarefas do TickTick — marcar aqui, não no TickTick)

- [x] Componente: App
  - Evidência: criados `src/components/` + `layout.module.js` / `element.module.js` (stub) / `components.module.js`; `geApp` com 3 arquivos do contrato (`app.component.js`, `app.html`, `app.component.spec.js`) — **sem** `app.theme.js` (exceção §5/§6); confirmado na tag Nuxt UI `v4.10.0` ausência de `src/theme/app.ts` e `App.vue` só providers (ConfigProvider/Tooltip/Toaster/Overlay). Stub: transclude + `geColorMode.set(get())` no `$onInit`. Wiring em `gravity-elements.module.js`, `src/index.js`, `test/karma.conf.js`. `npm test` → 39 of 39 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geApp` em `gravityElements.layout` / `gravityElements.components`.
- [x] Componente: Container
  - Evidência: criados `src/components/layout/container/` com 4 arquivos do contrato (`container.component.js`, `container.html`, `container.theme.js`, `container.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/container.ts` (confirmado `base: 'w-full max-w-(--ui-container) mx-auto px-4 sm:px-6 lg:px-8'`), normalizado para `slots.base` + adaptação TW3 `max-w-[var(--ui-container)]`; `variants.size` (sm–2xl) como extensão Gravity §6 (upstream sem prop size); `--ui-container: 80rem` em `gravity-elements.css`; wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 41 of 41 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geContainer`/`geContainerTheme` em `gravityElements.layout`; `npm run build:css` → safelist 11 classes (1 theme), CSS inclui `max-w-[var(--ui-container)]` e `max-w-screen-*`.
  - **Correção retroativa descoberta nesta revisão (Claude/Cowork, fora do fluxo Cursor)**: `window.twMerge` nunca era setado no bundle publicado — só no shim de teste do Karma (`test/shims/tw-merge-export.js`), então `geTv` (Etapa 0) degradava silenciosamente para "sem merge" (`identityMerge`) em produção. Passou despercebido na Etapa 0 porque nenhum smoke test tinha classes conflitantes no mesmo slot; `geContainer` com `size="lg"` foi o primeiro caso real (`max-w-[var(--ui-container)]` da base + `max-w-screen-lg` da variante, coincidentemente correto por ordem do CSS gerado, mas sem garantia). Corrigido em `src/index.js` (`import { twMerge } from 'tailwind-merge'; window.twMerge = twMerge;`, fora do Karma) + exceção `angular/window-service` adicionada em `.eslintrc.cjs` para esse arquivo, mesmo padrão já usado em `tv.service.js`. Validado via build isolado + execução real em `jsdom`: `typeof window.twMerge === 'function'` e `geContainer[size=lg].className` passou a ter só `max-w-screen-lg` (sem a classe conflitante da base). `npm run lint` exit 0.
- [x] Componente: Error
  - Evidência: criados `src/components/layout/error/` com 4 arquivos do contrato (`error.component.js`, `error.html`, `error.theme.js`, `error.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/error.ts` (slots root/leading/leadingIcon/statusCode/statusMessage/message/links), adaptação TW3 `text-primary`/`text-highlighted`/`text-muted` → `text-[var(--ui-*)]`; bindings §6 + extras `message`/`icon` (§5.4.2); `icon` como classe CSS inline (§5.4) e clear via `<button>` nativo aproximando UButton lg/primary/solid label "Back to home" (§5.4.1); CSS vars `--ui-header-height`/`--ui-primary`/`--ui-text-*` em `gravity-elements.css`; wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 43 of 43 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geError`/`geErrorTheme` em `gravityElements.layout`; `npm run build:css` → safelist 34 classes (2 themes), CSS inclui `min-h-[calc(100vh-var(--ui-header-height))]` e tokens.
- [x] Componente: Footer
  - Evidência: criados `src/components/layout/footer/` com 4 arquivos do contrato (`footer.component.js`, `footer.html`, `footer.theme.js`, `footer.component.spec.js`); tema portado 1:1 de Nuxt UI tag `v4.10.0` `src/theme/footer.ts` (7 slots: root/top/bottom/container/left/center/right, sem variants); multi-slot `transclude: { top: '?geFooterTop', left: '?geFooterLeft', right: '?geFooterRight', bottom: '?geFooterBottom' }` + default → center — justificado §5.3/§5.4.2 (mesma lógica do Error: tabela §6 era simplificação; slots do componente batem com o tema real / `Footer.vue`); DOM right→center→left + `ge-container` interno; top/bottom condicionais via `$transclude.isSlotFilled`. Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 45 of 45 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geFooter`/`geFooterTheme` em `gravityElements.layout`; `npm run build:css` → safelist 50 classes (3 themes), CSS inclui `lg:order-2` / `lg:justify-between`.
- [x] Componente: Header
  - Evidência: criados `src/components/layout/header/` com 4 arquivos do contrato (`header.component.js`, `header.html`, `header.theme.js`, `header.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/header.ts` — slots da barra estática (root/container/left/center/right/title), adaptação TW3 `bg-default/75`→`color-mix(--ui-bg)`, `border-default`→`border-[var(--ui-border)]`, `text-highlighted`→`text-[var(--ui-text-highlighted)]`, `h-(--ui-header-height)`→`h-[var(--ui-header-height)]`; **omitidos** slots `toggle`/`content`/`overlay`/`header`/`body` + `variants.toggleSide` (menu mobile Modal/Slideover/Drawer = Overlay Etapa 4; Angular não reutiliza left/right duas vezes — §5.4.2 / §10). Multi-slot §6: `transclude: { title: '?geHeaderTitle', left: '?geHeaderLeft', right: '?geHeaderRight', top: '?geHeaderTop', bottom: '?geHeaderBottom' }` + default → center; bindings `title`/`to` (§5.4.2). Tokens `--ui-bg`/`--ui-border` em `gravity-elements.css`. Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 47 of 47 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geHeader`/`geHeaderTheme` em `gravityElements.layout`; `npm run build:css` → safelist 66 classes (4 themes), CSS inclui `sticky`/`backdrop-blur`/`h-[var(--ui-header-height)]`/`color-mix`.
- [x] Componente: Main
  - Evidência: criados `src/components/layout/main/` com 4 arquivos do contrato (`main.component.js`, `main.html`, `main.theme.js`, `main.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/main.ts` (`base: 'min-h-[calc(100vh-var(--ui-header-height))]'`), normalizado para `slots.base` (sem adaptação TW3 — classe já compatível); sem bindings (§6); `<main>` + transclusion; ARIA sem requisito extra (§5.5). Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 49 of 49 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geMain`/`geMainTheme` em `gravityElements.layout`; `npm run build:css` → safelist 66 classes (5 themes; classe Main já coberta por `geErrorTheme`), CSS inclui `min-h-[calc(100vh-var(--ui-header-height))]`.
- [x] Componente: Sidebar
  - Evidência: criados `src/components/layout/sidebar/` com 4 arquivos do contrato (`sidebar.component.js`, `sidebar.html`, `sidebar.theme.js`, `sidebar.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/sidebar.ts` (13 slots root/gap/container/inner/header/wrapper/title/description/actions/close/body/footer/rail + variants side/collapsible/variant/transition + compoundVariants com arrays); adaptação TW3 `w-(--*)`→`w-[var(--*)]`, `divide/border/ring-default`→`[var(--ui-border)]`, `text-highlighted/muted`→tokens, `--spacing(N)`→rem; tokens `--sidebar-width`/`--sidebar-width-icon`/`--ui-border-accented` em `gravity-elements.css`. **Omitidos do template** (permanecem no tema): `rail` (aditivo §6) e overlay mobile Modal/Slideover/Drawer (Etapa 4, precedente Header). Bindings §6: `side`/`collapsible`/`variant`/`open` (`=?`)/`onToggle`/`onOpenChange` + extras `title`/`description` (§5.4.2); multi-slot header/title/description/actions/footer; toggle `<button>` nativo com `aria-expanded` (§5.4.1/§5.5). **Infra**: `geTv.compoundMatches` aceita array como OR + 2 testes em `tv.service.spec.js`. Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 53 of 53 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geSidebar`/`geSidebarTheme`; `npm run build:css` → safelist 141 classes (6 themes).
- [x] Componente: Theme
  - Evidência: criados `src/components/layout/theme/` com 3 arquivos do contrato (`theme.component.js`, `theme.html`, `theme.component.spec.js`) — **sem** `theme.theme.js` (exceção §5): confirmado na tag Nuxt UI `v4.10.0` ausência de `src/theme/theme.ts` (Theme.vue é headless provide/inject) e de tema próprio do ColorModeButton (estende Button). Spec §6: toggle claro/escuro via `geColorMode` + binding `mode` (`<`); botão nativo aproximando UButton md/neutral/ghost/square (§5.4.1) com `aria-label="Alternar tema claro/escuro"` (§5.5) e ícones CSS `dark:hidden` / `hidden dark:inline-block` (§5.4). Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 55 of 55 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geTheme` em `gravityElements.layout`; `npm run build:css` → safelist 141 classes (6 themes, inalterado).
- [x] Componente: Alert
  - Evidência: criados `src/components/element/alert/` com 4 arquivos do contrato (`alert.component.js`, `alert.html`, `alert.theme.js`, `alert.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/alert.ts` (slots root/wrapper/title/description/icon/avatar/avatarSize/actions/close + variants color/variant/orientation/title + compounds 6 cores×4 + neutral); adaptação TW3 `bg-/text-/ring-${color}` → `[var(--ui-*)]`, neutrals → `--ui-bg*`/`--ui-text-*`/`--ui-border*`; tokens `--ui-secondary|success|info|warning|error` + `--ui-bg-elevated`/`--ui-bg-inverted` em `gravity-elements.css`. Bindings §7 + extras `orientation`/`closeIcon` (§5.4.2); **omitidos do template** avatar/actions (slots no tema p/ safelist; dependem de geAvatar/geButton). `icon`/closeIcon classe CSS (§5.4) e close via `<button>` nativo aproximando UButton md/neutral/link (§5.4.1); `role="alert"` (§5.5); `closable`+`onClose` escondem o root. Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 57 of 57 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geAlert`/`geAlertTheme` em `gravityElements.element`; `npm run build:css` → safelist 180 classes (7 themes), CSS inclui `bg-[var(--ui-primary)]` / tokens de cor.
  - **Correção pós-revisão**: TW 3.4.19 não gera CSS para `bg-[var(...)]/N` nem `ring-[var(...)]/N` (outline/soft/subtle + neutral soft/subtle visualmente sem fundo/anel). Em `alert.theme.js`, opacidades trocadas pelo padrão `color-mix` do Header (`bg-/ring-[color-mix(in_srgb,var(--ui-*)_N%,transparent)]`). `npm run build:css` → regras reais `background-color:color-mix(... 10%/50%...)` e `--tw-ring-color:color-mix(... 25%...)`; safelist sem `/10`/`/25`/`/50`. `npm test` → 57 of 57 SUCCESS; `npm run lint` exit 0; `npm run build:js` OK.
  - **Correção pós-revisão (2026-08-08)**: `AlertController` só tinha `vm.$onInit` — `vm.classes` (via `geTv`) e derivados (`resolvedOrientation`, `resolvedCloseIcon`) computados uma única vez no mount; mudanças de `color`/`variant`/`orientation`/etc. depois de montado eram ignoradas (mesmo bug encontrado na revisão do Button, ver linha correspondente abaixo). Corrigido: lógica de `onInit` movida pra `render()`, chamada por `vm.$onInit` **e** `vm.$onChanges`; `vm.open = true` isolado só em `$onInit` (não pode rodar de novo em toda mudança de prop, senão reabriria um alert que o usuário já fechou). Novo caso de teste oficial em `alert.component.spec.js` (`atualiza classes quando color muda após montagem`). Revalidado via build isolado + jsdom (assert em `vm.classes`/DOM com *settle* de `ngAnimate` — seção 5.8 revisada): `npm test` equivalente 58 casos OK; `npm run lint` exit 0; `npm run build:js`/`build:css` OK.
- [x] Componente: Avatar
  - Evidência: criados `src/components/element/avatar/` com 4 arquivos do contrato (`avatar.component.js`, `avatar.html`, `avatar.theme.js`, `avatar.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/avatar.ts` (slots root/image/fallback/icon + variants color/size); adaptação TW3 `bg-${color}/10` → `color-mix` (precedente Alert/Header), `bg-elevated`/`text-muted`/`text-${color}` → tokens `--ui-*`. Bindings §7 + extra `color` (§5.4.2); fallback controller `src` → `text`/iniciais de `alt` → `icon` (§7; upstream Vue prioriza icon); `onError` na img; `icon` classe CSS (§5.4); chip inline aproximando UChip inset via `chipColor`/`chipPosition` (§5.4.1 até geChip); ARIA §5.5 (`alt` no `<img>`, `aria-hidden` no fallback, `aria-label` no root quando alt no fallback). Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 59 of 59 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geAvatar`/`geAvatarTheme` em `gravityElements.element`; `npm run build:css` → safelist 199 classes (8 themes).
- [x] Componente: AvatarGroup
  - Evidência: criados `src/components/element/avatar-group/` com 4 arquivos do contrato (`avatar-group.component.js`, `avatar-group.html`, `avatar-group.theme.js`, `avatar-group.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/avatar-group.ts` (slots root/base + variants size/color); adaptação TW3 `ring-bg`→`ring-[var(--ui-bg)]`, `ring-3`→`ring` (DEFAULT 3px). Bindings §7 `max`/`size` + extra `color` (§5.4.2 — provide/variant upstream). `$postLink`: conta filhos, reverse DOM (paridade Vue + `flex-row-reverse`), esconde excedente (`hidden`), prepend `ge-avatar` `+N` com `alt`/`aria-label` `"mais N"` (§5.5). `geAvatar` ganhou `require: '?^^geAvatarGroup'` para herdar size/color/base (overlap). 3 testes §5.6. Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 62 of 62 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geAvatarGroup`/`geAvatarGroupTheme` em `gravityElements.element`; `npm run build:css` → safelist 206 classes (9 themes).
- [x] Componente: Badge
  - Evidência: criados `src/components/element/badge/` com 4 arquivos do contrato (`badge.component.js`, `badge.html`, `badge.theme.js`, `badge.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/badge.ts` (slots base/label/leadingIcon/leadingAvatar/leadingAvatarSize/trailingIcon + variants color/variant/size/square/fieldGroup + compounds 6×4 + 4 neutral + 5 square); adaptação TW3 `bg-/text-/ring-${color}` → tokens `--ui-*`, opacidades `/10`/`/25`/`/50` → `color-mix` (precedente Alert/Header). Bindings §7 + extras `square`/`icon`/`leadingIcon`/`trailingIcon`/`leading`/`trailing` (§5.4.2); **omitidos do template** avatar/leadingAvatar (prop objeto; slots no tema p/ safelist); `fieldGroup` no tema sem wiring até geFieldGroup; `icon*` classe CSS (§5.4); `square` auto sem label/transclude (paridade Vue); token `--ui-text` em `gravity-elements.css`. Apagado `test/smoke/badge/` + 3 linhas smoke em `karma.conf.js`. Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 62 of 62 SUCCESS (−2 smoke +2 Badge); `npm run lint` exit 0; `npm run build:js` → UMD com `geBadge`/`geBadgeTheme` em `gravityElements.element`; `npm run build:css` → safelist 235 classes (10 themes), CSS com `color-mix` 10%/50% sem `/10` sobre `var()`.
  - **Correção pós-revisão (2026-08-08)**: mesmo bug de `$onChanges` ausente do Alert/Banner/Button — `vm.classes` computado só no `$onInit`. Corrigido: `onInit` renomeado pra `render`, registrado em `vm.$onInit` **e** `vm.$onChanges`. `geBadge` não tem efeito colateral tipo `vm.open`, então não precisou de isolamento extra. Novo caso de teste oficial em `badge.component.spec.js` (`atualiza classes quando color muda após montagem`, asserido em `vm.classes.base` — ver seção 5.8 revisada sobre por que DOM `className` não é confiável aqui). Revalidado via build isolado + jsdom: `npm test` equivalente OK; `npm run lint` exit 0; `npm run build:js`/`build:css` OK.
- [x] Componente: Banner
  - Evidência: criados `src/components/element/banner/` com 4 arquivos do contrato (`banner.component.js`, `banner.html`, `banner.theme.js`, `banner.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/banner.ts` (slots root/container/left/center/right/icon/title/actions/close + variants color/to + compounds hover quando to); adaptação TW3 `bg-${color}`/`bg-inverted`/`text-inverted` → tokens `--ui-*`, opacidades `/90`/`/10` e `outline-(--ui-bg)/25` → `color-mix` (precedente Alert/Header). Bindings §7 + extras `closeIcon`/`to` (§5.4.2); **omitidos** persistência `id`+localStorage/useHead (Nuxt SSR) e prop `actions[]` (transclusion até geButton). `icon`/closeIcon classe CSS (§5.4) e close via `<button>` nativo aproximando UButton md/neutral/ghost (§5.4.1); `ge-container` interno; ARIA §5.5 `role="alert"` se color error|warning senão `role="status"`. Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 64 of 64 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geBanner`/`geBannerTheme` em `gravityElements.element`; `npm run build:css` → safelist 252 classes (11 themes), CSS com `color-mix` sem `/90`/`/10` sobre `var()`.
  - **Correção pós-revisão**: TW 3.4.19 não gera CSS para `outline-3`/`-outline-offset-3` do variant `to` (escala de `outline-width` só 0/1/2/4/8 — causa raiz diferente do bug de opacidade/`color-mix` do Alert; upstream Nuxt UI v4.10.0 assume TW4). Em `banner.theme.js`, trocadas por `-outline-offset-[3px]` e `has-[>a:focus-visible]:outline-[3px]` (cor `outline-[color-mix(...)]` já ok). `npm run build:css` → regras reais `outline-width:3px` e `outline-offset:-3px` no dist; `npm test` → 64 of 64 SUCCESS; `npm run lint` exit 0; `npm run build:js` OK.
  - **Correção pós-revisão (2026-08-08)**: mesmo bug de `$onChanges` ausente do Alert/Badge/Button. Corrigido: lógica movida pra `render()`, chamada por `vm.$onInit` **e** `vm.$onChanges`; `vm.open = true` isolado só em `$onInit` (mesmo cuidado do Alert — não reabrir um banner já fechado pelo usuário ao mudar outra prop). Novo caso de teste oficial em `banner.component.spec.js` (`atualiza role e classes quando color muda após montagem`). Revalidado via build isolado + jsdom (com *settle* de `ngAnimate`, seção 5.8 revisada, incluindo o caso "fechar + mudar prop não reabre" testado à parte): `npm test` equivalente OK; `npm run lint` exit 0; `npm run build:js`/`build:css` OK.
- [x] Componente: Button
  - Evidência: criados `src/components/element/button/` com 4 arquivos do contrato (`button.component.js`, `button.html`, `button.theme.js`, `button.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/button.ts` (slots base/label/leadingIcon/leadingAvatar/leadingAvatarSize/trailingIcon + variants color/variant/size/block/square/leading/trailing/loading/active/fieldGroup + compounds 6×6 + 6 neutral + 5 square + 2 loading); adaptação TW3 tokens `--ui-*`, opacidades `/75`/`/90`/`/50`/`/25`/`/15`/`/10` → `color-mix` (precedente Alert/Badge), `focus-visible:outline-3` → `focus-visible:outline-[3px]` (precedente Banner). Bindings §7 + extras `icon`/`leadingIcon`/`trailingIcon`/`leading`/`trailing`/`loadingIcon`/`type` (§5.4.2); **omitidos** avatar (slots no tema p/ safelist), Link/`loadingAuto`/`fieldGroup` wiring; ícones classe CSS (§5.4); ARIA §5.5 `aria-busy` + `ng-disabled` quando loading. Token `--ui-bg-accented` em `gravity-elements.css`. Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 66 of 66 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geButton`/`geButtonTheme` em `gravityElements.element`; `npm run build:css` → safelist 387 classes (12 themes); CSS isolado + dist confirmam `background-color:var(--ui-primary)`, `color-mix(...75%...)` e `outline-width:3px`.
  - **Correção pós-revisão (2026-08-08)**: primeira versão só tinha `vm.$onInit` — `vm.classes`/`vm.isDisabled`/`vm.ariaBusy`/ícones resolvidos computados uma única vez no mount; confirmado via jsdom que `loading`/`color`/`variant`/`size`/etc. mudando depois do mount não atualizavam nada (`aria-busy`, `disabled`, spinner, cores todos congelados). Mesmo bug encontrado e corrigido em `geAlert`/`geBadge`/`geBanner` no mesmo ciclo de revisão. Corrigido: `onInit` renomeado pra `render`, chamada por `vm.$onInit` **e** `vm.$onChanges`; `geButton` não tem efeito colateral tipo `vm.open`, sem isolamento extra necessário. Revalidado via build isolado (lint/build:js/build:css) + jsdom: suíte de regressão dedicada (loading pós-montagem incluindo volta a `false`, color/variant/size pós-montagem via `vm.classes` — DOM `className` não confiável aqui, ver seção 5.8 revisada) + repetição da suíte funcional completa (square auto-detect, block, disabled bloqueando `onClick`, ícones leading/trailing simultâneos e exclusivos, `type`, `$transclude` chamado 2x por causa do novo `$onChanges` sem duplicar conteúdo) — todos os casos OK, nenhuma regressão.
- [x] Componente: Calendar
  - Evidência: criados `src/components/element/calendar/` com 4 arquivos do contrato (`calendar.component.js`, `calendar.html`, `calendar.theme.js`, `calendar.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/calendar.ts` (slots root/header/body/heading/headingLabel/grid/gridRow/gridWeekDaysRow/gridBody/headCell/headCellWeek/cell/cellTrigger/cellWeek + color/variant/size/view/weekNumbers + compounds); adaptação TW3 tokens `--ui-*`, opacidades `/N` → `color-mix`, `outline-3` → `outline-[3px]`, `data-selected:` → `data-[is-selected]:` (§5.10), `not-data-selected:` → `[&:not([data-is-selected])]:` / `[&[data-today]:not([data-is-selected])]:`, `text-md` → `text-base`. Só vista `day` no controller (month/year/weekNumbers no tema p/ safelist). Bindings §7 + extras `color`/`variant`/`size` (§5.4.2); matemática via `window.dateFns` (`date-fns` CDN no Karma + subset em `src/index.js` pro UMD); roving tabindex com `tabbable.focusable`; `$onInit`+`$onChanges`→`render()` sem resetar foco válido; ARIA §5.5 `role="grid|row|gridcell"`, `aria-selected`, `aria-label`/`aria-live`. Wiring em `src/index.js` e `test/karma.conf.js`. Confirmação manual no demo app fica pendente (tarefa critério de aceite §9.7 / Demo app).
  - **Correção pós-revisão (2026-08-08, §5.10)**: `ng-attr-data-selected` / `ng-attr-data-disabled` em `<button>` eram engolidos pelo `BOOLEAN_ATTR` do AngularJS 1.8.3 — atributo nunca ia pro DOM, então `data-[selected]:bg-*` / `data-[disabled]:text-*` do tema nunca ativavam (ARIA/`ng-disabled` ok). Renomeado pra `data-is-selected` / `data-is-disabled` no template + tema (`data-[is-selected]:` / `data-[is-disabled]:` / `[data-is-selected]`). Novo teste DOM `querySelector('[data-is-selected]')` / `[data-is-disabled]`. `npm test` → 78 of 78 SUCCESS; `npm run lint` exit 0; `npm run build:js`/`build:css` OK; CSS isolado confirma regras `[data-is-selected]`.
- [x] Componente: Card
  - Evidência: criados `src/components/element/card/` com 4 arquivos do contrato (`card.component.js`, `card.html`, `card.theme.js`, `card.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/card.ts` (slots root/header/title/description/body/footer + variants solid/outline/soft/subtle); adaptação TW3 tokens `--ui-*`, `bg-elevated/50` → `color-mix` (§5.7). **Decisão multi-slot** (como geFooter, 2026-08-07): `transclude: { header: '?geCardHeader', title: '?geCardTitle', description: '?geCardDescription', footer: '?geCardFooter' }` + default → body — justificado por Card.vue (`header`/`title`/`description`/`default`/`footer`) e tema com 6 slots (não slot único da tabela §7). Bindings extras §5.4.2: `variant`/`title`/`description` (tabela §7 tinha `—`). `$onInit`+`$onChanges`→`render()`; `hasBody` via probe `$transclude` (slot default não entra em `isSlotFilled`). Token `--ui-text-dimmed` em `gravity-elements.css`. Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 82 of 82 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geCard`/`geCardTheme` em `gravityElements.element`; `npm run build:css` → safelist 500 classes (14 themes); CSS isolado + dist confirmam `background-color:color-mix(...elevated 50%...)` e `background-color:var(--ui-bg)`.
- [x] Componente: Chip
  - Evidência: criados `src/components/element/chip/` com 4 arquivos do contrato (`chip.component.js`, `chip.html`, `chip.theme.js`, `chip.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/chip.ts` (slots root/base + variants color/size/position/inset/standalone + 4 compoundVariants translate); adaptação TW3 `ring-bg`→`ring-[var(--ui-bg)]`, `text-inverted`→`text-[var(--ui-text-inverted)]`, `bg-${color}`→`[var(--ui-*)]`, neutral `bg-inverted`→`bg-[var(--ui-bg-inverted)]` — §5.7 N/A (sem opacidade/`var()/N`, sem `ring-N`/`outline-N` fora da escala, sem `not-*`); §5.10 N/A (só `div`/`span`). Bindings §7 `text`/`label`/`color`/`size`/`position`/`standalone` + extras `inset`/`show` (§5.4.2 — Chip.vue); `label` alias de `text`; transclude default (filho decorado); `$onInit`+`$onChanges`→`render()`; defaults Vue `standalone=false`/`inset=false`/`show=true`. Avatar placeholder `chipColor`/`chipPosition` intacto (fora de escopo). Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 85 of 85 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geChip`/`geChipTheme` em `gravityElements.element`; `npm run build:css` → safelist 531 classes (15 themes); CSS confirma `background-color:var(--ui-primary)` e `height:8px`.
- [x] Componente: Collapsible
  - Evidência: criados `src/components/element/collapsible/` com 4 arquivos do contrato (`collapsible.component.js`, `collapsible.html`, `collapsible.theme.js`, `collapsible.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/collapsible.ts` (slots root/content); `animate-[collapsible-down|up]` (Reka `--reka-collapsible-content-height`) substituído por classe `ge-collapsible` + CSS ngAnimate em `gravity-elements.css` (`.ge-collapsible.ng-enter/leave` + aliases `.ge-collapsible-enter/leave`, max-height 200ms ease-out) — §5.8; mantido `data-[state=closed]:overflow-hidden`. §5.7 N/A (sem opacidade/`var()/N`, sem ring/outline fora da escala, sem `not-*`). §5.10: `data-state="open|closed"` (não `data-open`) em `div` — sem colisão BOOLEAN_ATTR. Bindings §7 `modelValue`/`onUpdate`/`disabled` + extras `defaultOpen`/`unmountOnHide` (§5.4.2); multi-slot `content: '?geCollapsibleContent'` + default=trigger; `$onInit`+`$onChanges`→`render()`; ARIA §5.5 `aria-expanded`/`aria-controls` (geId)/`aria-hidden`. 3 testes §5.6 + 1 pós-montagem (settle ngAnimate). Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 89 of 89 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geCollapsible`/`geCollapsibleTheme` em `gravityElements.element`; `npm run build:css` → safelist 533 classes (16 themes); CSS isolado + dist confirmam `overflow:hidden` em `.data-\[state\=closed\]\:overflow-hidden` e regras `.ge-collapsible.ng-enter`/`max-height`.
- [x] Componente: FieldGroup
  - Evidência: criados `src/components/element/field-group/` com 4 arquivos do contrato (`field-group.component.js`, `field-group.html`, `field-group.theme.js`, `field-group.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/field-group.ts` (slots.base `relative` + variants size vazios + orientation horizontal/vertical; `base` top-level → `slots.base`); bindings §7 `size` + extra `orientation` (§5.4.2 — FieldGroup.vue); `transclude: true`; `$onInit`+`$onChanges`→`render()`; filhos `geBadge`/`geButton` com `require: '?^^geFieldGroup'` (size próprio vence; `fieldGroup: orientation` no `geTv`); variant `fieldGroup` reescrito §5.7 `not-*` → seletores no host Angular (`[ge-button|ge-badge:not(:only-child):first-child_&]:…` — `[&:not(:only-child):first-child]` seria inerte no elemento interno); limitação §5.9 documentada (size/orientation do grupo pós-mount não re-renderiza filhos). 4 testes (default, orientation vertical, pós-montagem orientation, integração ge-button size/fieldGroup). Fix colateral: `settle` do Collapsible (done.fail no callback errado + assert em `vm`). Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 93 of 93 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geFieldGroup`/`geFieldGroupTheme`; `npm run build:css` → safelist 540 classes (17 themes); CLI isolado + dist confirmam `border-start-end-radius:0` / `border-radius:0` nos seletores `ge-button|ge-badge:not(:only-child)…` e `-space-x-px`.
- [x] Componente: Icon
  - Evidência: criados `src/components/element/icon/` com 4 arquivos do contrato (`icon.component.js`, `icon.html`, `icon.theme.js`, `icon.component.spec.js`); **sem** port de `theme/icon.ts` (ausente na tag Nuxt UI `v4.10.0` — Icon.vue só encaminha `name`/`size` px para `@nuxt/icon`); `geIconTheme` decisão interna: `slots.base` `shrink-0 inline-block` + `variants.size` 3xs–3xl (`size-3`/`size-3.5`/`size-4`/`size-4`/`size-5`/`size-5`/`size-6`/`size-7`/`size-8`, xs–xl alinhados aos leadingIcon do geButton); bindings §7 `name`/`size` (`@`); passthrough CSS sem sistema de ícones (§5.4); `$onInit`+`$onChanges`→`render()` via `geTv`; `aria-hidden="true"`; §5.7/§5.10 N/A. 3 testes (name como classe, size default/override, pós-montagem size). Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 96 of 96 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geIcon`/`geIconTheme` em `gravityElements.element`; `npm run build:css` → safelist 542 classes (18 themes); CSS inclui `size-3`…`size-8` / `size-3.5` / `shrink-0` / `inline-block`.
- [x] Componente: Kbd
  - Evidência: criados `src/components/element/kbd/` com 4 arquivos do contrato (`kbd.component.js`, `kbd.html`, `kbd.theme.js`, `kbd.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/kbd.ts` (slots.base + variants color/variant/size sm|md|lg + compoundVariants 6×4 + 4 neutral); adaptação TW3 tokens `--ui-*`, opacidades `/N` → `color-mix` (precedente geBadge — §5.7); defaults `outline`/`neutral`/`md`. Decisão API §7: só binding `value` (`@`) — paridade Kbd.vue; combinações = vários `<ge-kbd>`; transclusion opcional; mapa estático `KBD_KEYS_MAP` (useKbd.ts) sem detecção macOS para meta/alt/ctrl. `$onInit`+`$onChanges`→`render()`. 3 testes (shift→⇧, color/variant/size via geTv, pós-montagem value/color). Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 99 of 99 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geKbd`/`geKbdTheme`; `npm run build:css` → safelist 550 classes (19 themes); CLI isolado + dist confirmam `background-color:color-mix(...primary 10%...)`, `--tw-ring-color:var(--ui-border-accented)`, `min-w-[20px]`/`text-[11px]`.
- [x] Componente: Progress
  - Evidência: criados `src/components/element/progress/` com 4 arquivos do contrato (`progress.component.js`, `progress.html`, `progress.theme.js`, `progress.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/progress.ts` (slots root/base/indicator/status; omitidos steps/step + variants orientation/inverted/animation e compounds de animação — §7 barra simples); adaptação TW3 `bg-accented`→`bg-[var(--ui-bg-accented)]`, `text-dimmed`→`text-[var(--ui-text-dimmed)]`, `bg-${color}`→`[var(--ui-*)]`, neutral `bg-inverted`→`bg-[var(--ui-bg-inverted)]`; compoundVariants de altura horizontal embutidos só por `size` (sem chave orientation). **Decisão indeterminate**: estado visual quando `value` null/omitido (`data-state="indeterminate"`, sem `aria-valuenow`, indicador sem transform); feedback = uma animação `data-[state=indeterminate]:animate-pulse` — prop `animation` e 4 variantes carousel/swing/elastic fora do binding contract. Bindings §7 `value`/`max`/`color`/`size`/`status`; defaults `primary`/`md`/`max=100`; `$onInit`+`$onChanges`→`render()`; ARIA §5.5 `role="progressbar"` + valuemin/max/now. §5.7/§5.10 N/A (sem `/N` sobre `var()`, sem ring/outline fora da escala, sem `not-*`; `data-state` em `div`). 3 testes (determinate aria/%/transform, indeterminate omite aria-valuenow + data-state, pós-montagem value). Fix colateral: `collapsible.component.spec.js` usa `triggerHandler('click')` em vez de `click()` nativo (Chrome Headless não disparava ng-click no `<div>` trigger — settle 2s). Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 102 of 102 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geProgress`/`geProgressTheme` em `gravityElements.element`; `npm run build:css` → safelist 560 classes (20 themes); CLI isolado + dist confirmam `background-color:var(--ui-bg-accented|primary|inverted)`, `color:var(--ui-text-dimmed)` e `animation:pulse` em `.data-\[state\=indeterminate\]\:animate-pulse`.
- [x] Componente: Separator
  - Evidência: criados `src/components/element/separator/` com 4 arquivos do contrato (`separator.component.js`, `separator.html`, `separator.theme.js`, `separator.component.spec.js`); tema portado de Nuxt UI tag `v4.10.0` `src/theme/separator.ts` (slots root/border/container/label + icon/avatar/avatarSize no tema p/ safelist; variants color/orientation/size/position/type + compoundVariants position×orientation e size×orientation); adaptação TW3 `text-default`→`text-[var(--ui-text)]`, `border-${color}`→`border-[var(--ui-*)]`, neutral `border-default`→`border-[var(--ui-border)]` — §5.7 N/A (cores sólidas em `border-`, sem `/N` sobre `var()`, sem ring/outline, sem `not-*`); §5.10 N/A (sem `ng-attr-data-*`). Bindings §7 `orientation`/`label`/`color`/`size`/`type`; **omitidos** `icon`/`avatar`/`position` (não estão no §7 — só `label` como conteúdo); `position: 'center'` fixo internamente no `geTv`; `type` inclui `dotted` (paridade tema upstream). `$onInit`+`$onChanges`→`render()`; ARIA §5.5 `role="separator"` + `aria-orientation`. 3 testes (default ARIA/classes, label+override vertical/primary/md/dashed, pós-montagem orientation/color/label). Wiring em `src/index.js` e `test/karma.conf.js`. `npm test` → 105 of 105 SUCCESS; `npm run lint` exit 0; `npm run build:js` → UMD com `geSeparator`/`geSeparatorTheme` em `gravityElements.element`; `npm run build:css` → safelist 584 classes (21 themes); CLI isolado + dist confirmam `border-color:var(--ui-border|primary)`, `border-top-width:1px|2px`, `border-inline-start-width:3px`, `border-style:solid|dashed`.
- [ ] Componente: Skeleton
- [ ] Demo app: 1 rota por componente desta etapa
- [ ] [Critério de aceite] Comparação visual com ui.nuxt.com sem divergência perceptível
- [ ] [Critério de aceite] Calendar navagável por teclado
